# --- data ingestion ---
aiohttp
certifi
nest-asyncio
python-dotenv
tqdm
ipykernel

# --- numeric stack (native numpy 2) ---
# The default price encoder is the lightweight transformer backend, so nothing
# here requires numpy<2. We deliberately stay on numpy 2 to match Colab's
# prebuilt torch/pandas/pyarrow/scipy wheels (all numpy-2-built) — pinning
# numpy<2 for Moirai's uni2ts caused a cascade of C-ABI / numpy-corruption
# failures on Colab. Leave these unpinned (numpy 2 line).
numpy
pandas
pyarrow
scipy
scikit-learn

# --- JEPA model / training ---
torch
matplotlib
transformers>=4.44   # text encoder (FinBERT) + Qwen LoRA predictor
peft>=0.11
accelerate
einops

# --- Moirai price encoder (OPTIONAL — backend='moirai') ---
# uni2ts requires numpy<2 and is INCOMPATIBLE with the numpy-2 stack above and
# with stock Colab. Only install it in a clean, dedicated numpy<2 environment
# (local GPU / container), and pin the whole numeric stack to numpy<2 there:
#   numpy==1.26.4 pandas==2.1.4 pyarrow==15.0.2 scipy==1.11.4 scikit-learn==1.4.2
# uni2ts>=1.1
